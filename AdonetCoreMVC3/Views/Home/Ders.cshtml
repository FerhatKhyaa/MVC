@model IEnumerable<AdonetCoreMVC3.Entities.Dersler>

@{
    ViewData["Title"] = "Ders";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Ders</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DersId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DersName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DersId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DersName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
